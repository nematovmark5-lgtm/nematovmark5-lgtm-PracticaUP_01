# Практические задания

В ходе учебной практики были реализованы следующие структуры данных в виде отдельных программных модулей:

## 📋 Список выполненных заданий

### **1. Статический массив**
**Основная цель:** Создание массива с фиксированной ёмкостью для освоения работы с непрерывными областями памяти.  
**Реализованные методы:** `pushBack`, `pushFront`, `insert`, `remove`, `find`  
**Временная сложность:** O(1) для pushBack, O(n) для остальных операций  
**Ключевые аспекты:** Контроль границ массива, прямое управление памятью

### **2. Динамический массив**  
**Основная цель:** Реализация массива с автоматическим расширением для преодоления ограничений статической версии.  
**Реализованные методы:** Базовые методы + `_resize`  
**Временная сложность:** Амортизированная O(1) при добавлении элементов  
**Ключевые аспекты:** Стратегия геометрического расширения (×2), автоматический ресайзинг

### **3. Односвязный список**
**Основная цель:** Изучение структуры на основе узлов с однонаправленными связями.  
**Реализованные методы:** `pushFront`, `pushBack`, `remove`, `find`, `reverse`  
**Временная сложность:** O(1) для добавления в начало, O(n) для поиска  
**Ключевые аспекты:** Алгоритм разворота на месте (in-place)

### **4. Двусвязный список**
**Основная цель:** Реализация списка с двунаправленными связями для расширения функциональности.  
**Реализованные методы:** `pushBack`, `insertAfter`, `removeNode`, `find`, итератор  
**Временная сложность:** O(1) для удаления узла (при наличии ссылки)  
**Ключевые аспекты:** Удаление по прямой ссылке, поддержка двунаправленного обхода

### **5. Стек**
**Основная цель:** Создание структуры LIFO на различных базовых контейнерах.  
**Реализованные методы:** `push`, `pop`, `peek`, `isEmpty`  
**Временная сложность:** O(1) для всех операций  
**Ключевые аспекты:** Две реализации (массив и список), проверка корректности скобок

### **6. Очередь**
**Основная цель:** Реализация структуры FIFO с использованием разных подходов.  
**Реализованные методы:** `enqueue`, `dequeue`, `peek`, `isEmpty`  
**Временная сложность:** O(1) (амортизированная)  
**Ключевые аспекты:** Кольцевой буфер, реализация через два стека

### **7. Калькулятор выражений**
**Основная цель:** Применение стека для обработки арифметических выражений.  
**Реализованные методы:** `infixToRPN`, `evaluateRPN`, `calculate`  
**Временная сложность:** O(n) для преобразования и вычисления  
**Ключевые аспекты:** Алгоритм "Shunting yard", поддержка приоритетов операций

### **8. Хэш-таблица**
**Основная цель:** Создание структуры для эффективного поиска с обработкой коллизий.  
**Реализованные методы:** `put`, `get`, `remove`, `_hash`, `visualize`  
**Временная сложность:** O(1) в среднем случае  
**Ключевые аспекты:** Метод цепочек, автоматическое рехеширование

### **9. Частотный словарь**
**Основная цель:** Применение хэш-таблицы для анализа текстовой информации.  
**Реализованные методы:** `build_with_hash`, `build_with_counter`, `get_top_words`  
**Временная сложность:** O(n) для построения  
**Ключевые аспекты:** Сравнение хэш-функций, определение популярных слов

### **10. Система автодополнения**
**Основная цель:** Реализация префиксного дерева для интеллектуального поиска.  
**Реализованные методы:** `insert`, `autocomplete`, `get_suggestions_by_frequency`  
**Временная сложность:** O(m) для поиска по префиксу  
**Ключевые аспекты:** Комбинация Trie и HashMap, ранжирование по частоте

### **11. Бинарное дерево поиска**
**Основная цель:** Создание сбалансированного дерева для хранения упорядоченных данных.  
**Реализованные методы:** `insert`, `search`, `delete`, различные обходы  
**Временная сложность:** O(log n) для операций (при балансировке)  
**Ключевые аспекты:** Балансировка, вращения, три типа обхода

### **12. Расширенное префиксное дерево**
**Основная цель:** Дополнение функциональности Trie дополнительными операциями.  
**Реализованные методы:** `starts_with`, `delete`, `get_word_count`, `get_stats`  
**Временная сложность:** O(m) для основных операций  
**Ключевые аспекты:** Расширенная статистика, удаление элементов

### **13. Алгоритмы на графах**
**Основная цель:** Реализация представлений графов и базовых алгоритмов обхода.  
**Реализованные методы:** `bfs`, `dfs`, `shortest_path_unweighted`  
**Временная сложность:** O(V + E) для обходов  
**Ключевые аспекты:** Два представления (матрица и список смежности)

### **14. Задача "Острова"**
**Основная цель:** Применение алгоритмов обхода для анализа бинарных матриц.  
**Реализованные методы:** `count_islands_dfs`, `count_islands_bfs`, `get_island_sizes`  
**Временная сложность:** O(rows × cols)  
**Ключевые аспекты:** Поиск компонент связности, работа с 8 направлениями

### **15. Бинарная куча**
**Основная цель:** Реализация структуры для быстрого доступа к экстремальным элементам.  
**Реализованные методы:** `insert`, `extract_min`, `build_heap`, `is_valid_heap`  
**Временная сложность:** O(log n) для основных операций  
**Ключевые аспекты:** Построение из массива, проверка свойств кучи

### **16. Приоритетная очередь**
**Основная цель:** Создание очереди с приоритетом на основе бинарной кучи.  
**Реализованные методы:** `push`, `pop`, `task_scheduling`, `find_k_smallest`  
**Временная сложность:** O(log n) для вставки и удаления  
**Ключевые аспекты:** Планирование задач, поиск k минимальных элементов

---

## 🗂️ Организация проекта

Код проекта структурирован следующим образом:
- Каждое задание реализовано в виде отдельного модуля Python
- Все модули содержат исчерпывающие тесты и примеры использования
- Документация оформлена в соответствии с PEP 257 (docstrings)
- Проект размещён в Git-репозитории с подробной историей разработки

## 📊 Объём выполненной работы

Всего в рамках практики было:
- Реализовано 16 различных структур данных
- Написано более 2 500 строк кода на Python
- Создано более 150 unit-тестов
- Подготовлена полная документация по каждому модулю

Работа над проектом позволила глубоко понять внутреннее устройство основных структур данных, их сильные и слабые стороны, а также области практического применения.